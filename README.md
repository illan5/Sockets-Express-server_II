

# Socket-Server

Install node modules
```
npm install
```

Generate dist
```
tsc -w
```

Run server (one of the follow commands, not both)
```
nodemon dist/
node dist/
```

Install node modules
```
npm install
```